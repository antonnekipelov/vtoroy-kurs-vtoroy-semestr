procedure obmen (var a, b:integer);
var max:integer;
  begin
    max:=a;
    a:=b;
    b:=max
  end;
var
  a,b,c:integer;
begin
  read(a,b,c);
  if a>b then obmen(a,b);
  if b>c then obmen (b,c);
  if a>b then obmen(a,b);
  write(a,' ',b,' ',c);
end.
